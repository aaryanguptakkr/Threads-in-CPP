#include <stdio.h>
#include <stdlib.h>

int main(){
    int a = 5;
    int b = 6;
    swap(a, b);
    printf("after swapping %d and %d",a,b);
}
void swap(int a, int b){
    int temp;
    temp = b;
    b = a;
    a = temp;
}
